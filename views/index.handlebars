{{! <body> }}
<header>
  <nav>
    <ul>
      <li><a href='#'>Tabaco</a></li>
      {{! <li><a href="https://tabacoai-19733de2ea98.herokuapp.com/">AITabaco</a></li> }}
      <li><a href='/'>ImgTabaco</a></li>
      {{! <li><a href="https://tabacoai-19733de2ea98.herokuapp.com/user/pnikolenko">PNikolenko</a></li> }}
    </ul>
  </nav>
  
</header>
<br />
<form action='/' method='post'>
  <input
    type='text'
    class='form-control mb-2'
    placeholder='Enter img params'
    required
    name='prompt'
  />

  {{! <select name='size' class='form-control mb-2'> }}
  {{! <option value='256x256'>Small</option> }}
  {{! <option value='512x512'>Medium</option>
    <option value='1024x1024'>Large</option> }}
  {{! </select>  }}

  <input type='number' class='form-control mb-2' name='number' value='1' />
  {{! <button type='submit' class='btn btn-primery'>Generate</button> }}
   <input type='submit' class='form-control mb-2'></input> 
</form>

<div class='center-images'>
{{#if Images}}
    {{#each Images}}
      <img src='{{this.url}}' class='img-fluid rounded save-image' data-url='{{this.url}}' />
    {{/each}}
{{/if}}
</div>

  <h1>Tabaco Img World</h1>
  <p>вводите описание картинки и колличество до девяти штук.</p>

{{!-- <script>
  $('.save-image').on('click', function() {
    const imageUrl = $(this).data('url');

    $.ajax({
      url: '/saveImage',
      method: 'POST',
      data: { url: imageUrl },
      success: function(response) {
        console.log('Image saved successfully');
      },
      error: function(error) {
        console.log('Failed to save image', error);
      }
    });
  });
</script> --}}

<script>
$('.save-image').on('click', function() {
  const imageUrl = $(this).attr('src');
  // Сохраняем ссылку на элемент изображения
  const imgElement = $(this);

  $.ajax({
    url: '/saveImage',
    method: 'POST',
    data: { url: imageUrl },
    success: function(response) {
      console.log('Image saved successfully');
      // Изменение прозрачности изображения при успешном сохранении
      imgElement.css('opacity', '0.5');
    },
    error: function(error) {
      console.log('Failed to save image', error);
    }
  });
});
</script>

